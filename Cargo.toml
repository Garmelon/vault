[package]
name = "vault"
version = "0.5.0"
edition = "2021"

[features]
serde = ["dep:serde"]
tokio = ["dep:tokio"]

[dependencies]
rusqlite = "0.40.1"
serde = { version = "1.0.229", optional = true }
tokio = { version = "1.53.1", features = ["sync"], optional = true }
